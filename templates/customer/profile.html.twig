{%  extends 'customer_layout.html.twig' %}

{% block title %}
{{ parent() }} - Account
{% endblock %}


            {% block navOptions %}
            {{parent()}}
            {% endblock %}


{% block body %}
    {{ parent() }}
    
    <section id="postal-banner" class="section postal-banner">
        <div class="container">

            {% for crumbsKey, crumbsValue in breadcrumbs %}
                <a href={{ crumbsValue }}>{{ crumbsKey}}</a> <strong>/</strong>
            {% endfor %}

            <div class="title">Your Courier.PO Profile</div>
            <div class="subtitle">Set your account details or change your address. </small>
            </div>
        </div>

    </section>

    <section class="section is-small">
        <div class="container">
        <div class="columns">

            <div class="column is-three-quarters">

                {{ form(profile )}}

            </div>
            
        </div>
        </div>
    </section>
    
{% endblock %}