{%  extends 'employee_layout.html.twig' %}

{% block title %}
{{ parent() }} - Account
{% endblock %}


            {% block navOptions %}
            {{parent()}}
            {% endblock %}


{% block body %}
    {{ parent() }}
    
    <section id="postal-banner" class="section postal-employee-banner">
        <div class="container">
            <div>
            
            {% for crumbsKey, crumbsValue in breadcrumbs %}
                <a href={{ crumbsValue }}>{{ crumbsKey}}</a> <strong>/</strong>
            {% endfor %}
            
            </div>
            <div class="title">Your Courier.PO Profile</div>
            <div class="subtitle">Employee ID: {{ id }}</div>
        </div>

    </section>
    
{% endblock %}